mixin videoBlock(video={})
    .videoBlock
        a(href=routes.videoDetail(video.id)) Video Detail
        video.videoBlock__thumnail(src=video.videoFile, autoplay=true, controls=true)
        h4.videoBlock__title=video.title
        h6.videoBlock__views=video.views
