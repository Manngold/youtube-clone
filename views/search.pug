extends layouts/main.pug
include mixins/videoBlock

block content
    .search__header
        h3 Result about #{searchingBy}
    .search__videos
        each video in videos
            +videoBlock({
                title: video.title,
                views: video.views,
                videoFile: video.videoFile,
            })